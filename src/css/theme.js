const theme = {
    mainColor: 'red',
    subColor: 'orange',
    dangerColor: 'blue',
    successColor: 'gray',
    keyColor: 'rgb(255, 44, 17)',

    // font-color
    fontColorBase: '#333',

    // font-size
    fontSize: '24px',

    // border-radius
    radius: {
        default: '10px',
    },

    // box-shadow
    boxShadow: {
        default: '0 20px 20px rgba(0, 0, 0, 0.2)',
    },
};

export default theme;
