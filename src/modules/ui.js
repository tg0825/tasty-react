// ui handling
// const FOO = 'FOO';

const initialState = {};

// export const foo = () => ({
//     type: FOO,
// });

// reducer
const ui = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default ui;
